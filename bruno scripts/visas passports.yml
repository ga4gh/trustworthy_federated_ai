info:
  name: visas passports
  type: http
  seq: 13

http:
  method: GET
  url: http://localhost:4500/ga4gh/passport/v1/service-info
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
