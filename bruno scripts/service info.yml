info:
  name: service info
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:4500/ga4gh/drs/v1/service-info
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
